# docker/

**Placeholder — not active in this milestone.**

A containerized setup (Docker Compose with PostgreSQL + Python workers) is a
planned future milestone. Nothing is written here yet.

The active deployment target is a bare on-prem VM with a native PostgreSQL
install and a Python virtualenv. See `docs/setup.md` (Milestone 7).
