# scripts/

**Placeholder — Milestone 6+.**

Utility shell scripts for common DBA and operator tasks:

| Script | Purpose |
|--------|---------|
| `apply_migrations.sh` | Apply all pending migrations in `database/migrations/` |
| `reset_db.sh` | Drop and recreate the `orderflow` schema (dev/test only) |
| `check_invariants.sh` | Run SQL queries verifying INV-06 and other business invariants |
