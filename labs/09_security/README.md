# Lab 09 — Security

**Status:** Not Started (prerequisite: Lab 08)

Row-Level Security on `employees` and `customers`. pgcrypto column
encryption on `employees.salary` and customer PII. pgaudit audit trail
on `payments`. SSL/TLS connections and client certificates. Application
roles and `SET ROLE`.
