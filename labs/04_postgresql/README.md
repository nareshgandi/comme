# Lab 04 — PostgreSQL Core

**Status:** Not Started (prerequisite: Lab 03)

Transactions and MVCC, generated columns (`order_items.line_total`),
the `updated_at` trigger, FK cascade deletes, savepoints, and isolation
levels — all demonstrated against the live OrderFlow workload.
