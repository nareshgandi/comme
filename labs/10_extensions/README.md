# Lab 10 — Extensions

**Status:** Not Started (prerequisite: Lab 09)

pg_cron: schedule a FAILED-payment retry job. pg_partman: automate future
partition creation for `orders`. pgvector: add a product embedding column
and run semantic similarity search. postgres_fdw: federate warehouse data
across simulated regions.
